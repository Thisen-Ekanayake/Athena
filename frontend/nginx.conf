server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Serve static assets with long cache
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # All other routes fall through to index.html for SPA routing
    location / {
        try_files $uri $uri/ /index.html;
    }
}
